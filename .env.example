# Docker Images
# Specify the Docker images for Blue and Green deployments
BLUE_IMAGE=yimikaade/wonderful:devops-stage-two
GREEN_IMAGE=yimikaade/wonderful:devops-stage-two

# Active Pool Configuration
# Set which pool is initially active: 'blue' or 'green'
ACTIVE_POOL=blue

# Release Identifiers
# These are passed to the containers and included in logs/alerts
RELEASE_ID_BLUE=blue-v1.0.0
RELEASE_ID_GREEN=green-v1.0.0

# Application Port
# The internal port the app listens on (defaults to 3000 if not set)
PORT=3000

# ============================================
# Stage 3: Monitoring and Alerting
# ============================================

# Slack Webhook URL
# Create an incoming webhook at https://api.slack.com/messaging/webhooks
# and replace with your actual webhook URL
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Error Rate Threshold (%)
# Alert when error rate exceeds this percentage over the sliding window
# Default: 2 (means 2% of requests result in 5xx errors)
ERROR_RATE_THRESHOLD=2

# Sliding Window Size
# Number of recent requests to consider when calculating error rate
# Default: 200 requests
WINDOW_SIZE=200

# Alert Cooldown Period (seconds)
# Minimum time between consecutive alerts of the same type to prevent spam
# Default: 300 seconds (5 minutes)
ALERT_COOLDOWN_SEC=300

# Maintenance Mode Flag
# Set to 'true' to suppress alerts during planned maintenance/toggles
# Set to 'false' for normal operation
# Default: false
MAINTENANCE_MODE=false
